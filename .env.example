# Leave unset for local mode (single-user, IndexedDB persistence).
# Set both to enable the team workspace (auth, shared projects, shared assets, presence).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
